const chart=document.querySelector('#scoreChart');
if(chart){const values=(chart.dataset.scores||'').split(',').filter(Boolean).map(Number),labels=(chart.dataset.labels||'').split(',');if(values.length){const w=760,h=280,p=38;const x=i=>values.length===1?w/2:p+i*(w-2*p)/(values.length-1);const y=v=>h-p-(v/1000)*(h-2*p);const points=values.map((v,i)=>`${x(i)},${y(v)}`).join(' ');chart.innerHTML=`<svg viewBox="0 0 ${w} ${h}" role="img" aria-label="Evolução das notas">${[0,250,500,750,1000].map(v=>`<line x1="${p}" y1="${y(v)}" x2="${w-p}" y2="${y(v)}" class="grid"/><text x="0" y="${y(v)+4}">${v}</text>`).join('')}<polyline points="${points}" class="chart-line"/>${values.map((v,i)=>`<circle cx="${x(i)}" cy="${y(v)}" r="6"><title>${labels[i]||''}: ${v} pontos</title></circle><text class="point-value" x="${x(i)}" y="${y(v)-13}" text-anchor="middle">${v}</text><text class="date" x="${x(i)}" y="${h-8}" text-anchor="middle">${labels[i]||''}</text>`).join('')}</svg>`;}}
